<ui:FluentWindow
    x:Class="RedmineClient.Views.Windows.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helper="clr-namespace:RedmineClient.Helpers"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:local="clr-namespace:RedmineClient.Views.Windows"
    xmlns:models="clr-namespace:RedmineClient.Models"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="RedmineClient - WBS"
    Width="1100"
    Height="650"
    d:DataContext="{d:DesignInstance local:MainWindow,
                                     IsDesignTimeCreatable=True}"
    d:DesignHeight="650"
    d:DesignWidth="1100"
    ExtendsContentIntoTitleBar="True"
    WindowBackdropType="Mica"
    WindowCornerPreference="Round"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <ui:FluentWindow.InputBindings>
        <!-- 一時的に無効化 -->
    </ui:FluentWindow.InputBindings>

    <i:Interaction.Behaviors>
        <helper:ResizeBehavior />
    </i:Interaction.Behaviors>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar x:Name="TitleBar" Title="RedmineClient - WBS" Grid.Row="0" CloseWindowByDoubleClickOnIcon="True">
            <ui:TitleBar.Icon>
                <ui:ImageIcon Source="pack://application:,,,/Assets/wpfui-icon-256.png" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>

        <!-- メニューバー -->
        <Menu Grid.Row="1" Background="{DynamicResource MenuBackgroundBrush}" Foreground="{DynamicResource MenuForegroundBrush}">
            <MenuItem Header="ファイル(_F)">
                <MenuItem Header="新規作成(_N)" InputGestureText="Ctrl+N" />
                <MenuItem Header="開く(_O)" InputGestureText="Ctrl+O" />
                <Separator />
                <MenuItem Header="保存(_S)" InputGestureText="Ctrl+S" />
                <MenuItem Header="名前を付けて保存(_A)" InputGestureText="Ctrl+Shift+S" />
                <Separator />
                <MenuItem Header="終了(_X)" />
            </MenuItem>
            <MenuItem Header="編集(_E)">
                <MenuItem Header="元に戻す(_U)" InputGestureText="Ctrl+Z" />
                <MenuItem Header="やり直し(_R)" InputGestureText="Ctrl+Y" />
                <Separator />
                <MenuItem Header="切り取り(_T)" InputGestureText="Ctrl+X" />
                <MenuItem Header="コピー(_C)" InputGestureText="Ctrl+C" />
                <MenuItem Header="貼り付け(_P)" InputGestureText="Ctrl+V" />
            </MenuItem>
            <MenuItem Header="表示(_V)">
                <MenuItem Header="ツールバー(_T)" IsCheckable="True" IsChecked="True" />
                <MenuItem Header="ステータスバー(_S)" IsCheckable="True" IsChecked="True" />
            </MenuItem>
            <MenuItem Header="ツール(_T)">
                <MenuItem Header="オプション(_O)" />
            </MenuItem>
            <MenuItem Header="ヘルプ(_H)">
                <MenuItem Header="バージョン情報(_A)" />
                <MenuItem Header="ヘルプ(_H)" InputGestureText="F1" />
            </MenuItem>
        </Menu>

        <ui:NavigationView
            x:Name="RootNavigation" Grid.Row="2" Padding="10,0,10,0"
            FrameMargin="0" IsBackButtonVisible="Visible" IsPaneOpen="False" IsPaneToggleVisible="True"
            OpenPaneLength="310" PaneDisplayMode="Left"
            TitleBar="{Binding ElementName=TitleBar, Mode=OneWay}">
            <!-- 一時的に無効化 -->
            <ui:NavigationView.ContentOverlay>
                <Grid>
                    <ui:SnackbarPresenter x:Name="SnackbarPresenter" />
                </Grid>
            </ui:NavigationView.ContentOverlay>
        </ui:NavigationView>

        <!--  フッター  -->
        <ui:Card Grid.Row="3" Margin="0,8,0,0" Padding="16,12" Background="White">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!--  左側：アプリケーション情報  -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <ui:TextBlock VerticalAlignment="Center" FontTypography="Caption" FontWeight="SemiBold" Foreground="Black" Text="RedmineClient" />
                    <ui:TextBlock Margin="8,0,0,0" VerticalAlignment="Center" FontTypography="Caption" Foreground="Gray" Text="v1.0.0" />
                </StackPanel>

                <!--  右側：ステータス情報  -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <ui:TextBlock VerticalAlignment="Center" FontTypography="Caption" Foreground="Black" Text="接続状態:" />
                    <ui:TextBlock Margin="4,0,0,0" VerticalAlignment="Center" FontTypography="Caption" FontWeight="SemiBold" Foreground="Black" Text="未接続" />
                    <ui:TextBlock Margin="8,0" VerticalAlignment="Center" FontTypography="Caption" Foreground="Black" Text="|" />
                    <ui:TextBlock VerticalAlignment="Center" FontTypography="Caption" Foreground="Black" Text="最終更新:" />
                    <ui:TextBlock Margin="4,0,0,0" VerticalAlignment="Center" FontTypography="Caption" FontWeight="SemiBold" Foreground="Black" Text="--" />
                </StackPanel>
            </Grid>
        </ui:Card>

        <ContentPresenter x:Name="RootContentDialog" Grid.Row="0" />

        <ui:ProgressRing x:Name="ProgressRing" IsIndeterminate="True" Visibility="Hidden" />
    </Grid>
</ui:FluentWindow>
